#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

echo "[pre-push] Running fast static checks"
bash tests/scripts/check.sh

echo "[pre-push] Running repository contract tests"
PYTHONPATH="$repo_root${PYTHONPATH:+:$PYTHONPATH}" bash tests/scripts/test.sh prepush
